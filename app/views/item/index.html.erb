<% form_for :user do |form| %>
  <fieldset>
    <legend>Your Total Credit </legend>
      <%= render :partial => "credit_field_row",
             :locals  => { :form => form, :items =>@items, :user => @user } %>
  </fieldset>
<% end %>
<br/>
<% form_for :item do |form| %>
  <fieldset>
    <legend>Your Transaction Details</legend>
      <%= render :partial => "header_field_row",
             :locals  => { :form => form, :all_items =>@all_items, :user => @user } %>
  </fieldset>
<% end %>
<br/><br/>
<%= link_to "Add New Transaction", :action => "new" %><br/><br/>
<%= link_to "View All User Credits", :controller => "user", :action => "show" %>